"""
Tuples are a in-build data types that lets us create immutable sequence of values
list is mutable where we can change the values but tuple is immutable we cannot change the values like strings 
here we use paranthesis--> () instead of []
example :
tup=(1, 2, 4, 5 ,6)
print(tup)
print(tuple)
to store single value in type -> tup = (1,) we have to use comma other wise it will consider as a numerical value which is enclosed in paranthesis
"""
# tup=(1, 2,3,3,4, 5, 3,6)
# # count() returns the no of occurences in tuple 
# print(tup.count(2))
# # index() returns index of first occurance 
# print(tup.index(3))


