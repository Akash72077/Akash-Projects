# Legacy / Optional Supabase Reference

This folder is retained from the uploaded CityResolve project so the original project files are preserved. The updated CivicPulse AI build uses the Node.js + Express REST backend in `/backend` by default, so Supabase setup is not required to run the demo.
